{
  "name": "kiip-mcp",
  "description": "Read-only MCP tools for the Kiip HR platform (persons, org, payroll) with in-Claude login.",
  "version": "0.2.7",
  "mcpServers": {
    "kiip": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.mjs"],
      "cwd": "${CLAUDE_PROJECT_DIR}"
    }
  }
}
